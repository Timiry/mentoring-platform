import { styled } from "@mui/material";
import { Typography } from "@mui/material";

const InvalidInputMessage = styled(Typography)(() => ({
  fontSize: "12px",
  color: "red",
}));

export default InvalidInputMessage;
